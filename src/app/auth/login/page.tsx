import { LoginForm } from './ui/LoginForm';

export const metadata = {
	title: 'Iniciar Sessión',
	description: 'Iniciar Sessión',
};
export default function LoginPage() {
	return (
		<>
			<LoginForm />
		</>
	);
}
